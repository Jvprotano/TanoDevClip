<section id="requirements" class="requirements-section" aria-labelledby="requirements-title">
  <div class="container">
    <h2 id="requirements-title" class="sr-only">System requirements</h2>
    <div class="terminal-card">
      <div class="titlebar">
        <div class="titlebar-title mono">system_requirements.json</div>
      </div>
      <div class="card-body mono" aria-label="System requirements for TanoDev Clip">
        <div class="line">
          <span class="key">"os"</span>: <span class="string">"Windows 10 or Windows 11"</span>,
        </div>
        <div class="line">
          <span class="key">"architecture"</span>: <span class="string">"x64"</span>,
        </div>
        <div class="line">
          <span class="key">"dotnet_runtime"</span>: <span class="string">"included"</span>,
        </div>
        <div class="line">
          <span class="key">"webview2"</span>: <span class="string">"installed by setup when required"</span>,
        </div>
        <div class="line">
          <span class="key">"account_required"</span>: <span class="boolean">false</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .requirements-section {
    padding: var(--space-2xl) 0;
  }

  .terminal-card {
    max-width: 680px;
    margin: 0 auto;
    background-color: var(--color-bg-root);
    border: 1px solid var(--color-border-strong);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .titlebar {
    background-color: rgba(14, 23, 27, 0.96);
    border-bottom: 1px solid var(--color-border-subtle);
    padding: 7px var(--space-md);
    text-align: center;
  }

  .titlebar-title {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .card-body {
    padding: var(--space-lg);
    overflow-x: auto;
    font-size: 0.9rem;
    line-height: 1.9;
  }

  .line {
    min-width: max-content;
  }

  .key {
    color: var(--color-action);
  }

  .string {
    color: var(--color-accent);
  }

  .boolean {
    color: var(--color-warning);
  }
</style>
